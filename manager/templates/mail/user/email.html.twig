{% extends 'mail.html.twig' %}

{% block body %}

    <p>Для родтверждения вашего email перейдите по ссылке:</p>

    {% set url = url('profile.email.confirm', {'token': token}) %}

    <p><a href="{{ url }}">{{ url }}</a></p>

{% endblock %}