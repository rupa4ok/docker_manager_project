{% extends 'mail.html.twig' %}

{% block body %}

    <p>Ссылка для восстановления пароля:</p>

    {% set url = url('auth.reset.reset', {'token': token}) %}

    <p><a href="{{ url }}">{{ url }}</a></p>

{% endblock %}